## Git 게임

https://learngitbranching.js.org/?locale=ko

- A 브랜치에서 `git merge B` -> A에는 merge이력이 남지만 B에는 안남음. 이 이후에 B브랜치에서 `git merge A`해야 두개의 브랜치의 log가 일치하게 됨
- A 브랜치에서 git rebase B -> A의 로그들을 B앞에 붙여버림 (복사본을)

